---
title: Saturation Area
description: 2D saturation/lightness picker area component
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import ComponentPreview from '@/components/ComponentPreview.vue'
import SaturationAreaDemo from '@/components/gallery/SaturationArea.vue'
import PropsTable from '@/components/PropsTable.astro'
import DataTable from '@/components/DataTable.astro'

A 2D interactive area for picking color saturation and lightness values.

<ComponentPreview client:load componentKey="saturation-area">
  <SaturationAreaDemo client:load />
</ComponentPreview>

## Usage

<Tabs syncKey="framework">
  <TabItem label="Vue">
  ```ts
  import { SaturationArea } from '@hueycolor/vue'
  ```
  ```vue
  <script setup>
  import { ref } from 'vue'
  import { HueyRoot, SaturationArea, hueyColor } from '@hueycolor/vue'

  const color = ref(hueyColor('#1A3263'))
  </script>

  <template>
    <HueyRoot v-model="color">
      <SaturationArea />
    </HueyRoot>
  </template>
  ```
  </TabItem>
  <TabItem label="Svelte">
  ```ts
  import { SaturationArea } from '@hueycolor/svelte'
  ```
  ```svelte
  <script>
  import { HueyRoot, SaturationArea, hueyColor } from '@hueycolor/svelte'

  let color = $state(hueyColor('#1A3263'))
  </script>

  <HueyRoot bind:color>
    <SaturationArea />
  </HueyRoot>
  ```
  </TabItem>
</Tabs>

## Props

<PropsTable rows={[
  { name: 'colorFormat', type: "Exclude<ColorFormat, 'oklch'>", default: "'hsl'", description: 'Controls whether to use HSL or HSV saturation/lightness.' },
]} />

Also extends all standard HTML attributes.

## Styling

Target the area with `[huey-area]`, and its draggable thumb with `[huey-area] [huey-slider-thumb]`.

<DataTable
  headers={['Property', 'Default', 'Description']}
  rows={[
    ['`--huey-area-width`', '`240px`', 'Width of the area'],
    ['`--huey-area-height`', '`230px`', 'Height of the area'],
    ['`--huey-area-radius`', '`12px`', 'Border radius of the area'],
    ['`--huey-thumb-size`', '`16px`', 'Size of the area thumb (set on `[huey-area]` to override)'],
  ]}
/>

The thumb's background color is driven dynamically by `--huey-thumb-color`; you typically don't need to set it.

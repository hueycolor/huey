---
title: Hex Input
description: Hex color input component
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import ComponentPreview from '@/components/ComponentPreview.vue'
import HexInputDemo from '@/components/gallery/HexInput.vue'
import PropsTable from '@/components/PropsTable.astro'

An input field for entering hexadecimal color values.

<ComponentPreview client:load componentKey="hex-input">
  <HexInputDemo client:load />
</ComponentPreview>

## Usage

<Tabs syncKey="framework">
  <TabItem label="Vue">
  ```ts
  import { HexInput } from '@hueycolor/vue'
  ```
  ```vue
  <script setup>
  import { ref } from 'vue'
  import { HueyRoot, HexInput, hueyColor } from '@hueycolor/vue'

  const color = ref(hueyColor('#1A3263'))
  </script>

  <template>
    <HueyRoot v-model="color">
      <HexInput />
    </HueyRoot>
  </template>
  ```
  </TabItem>
  <TabItem label="Svelte">
  ```ts
  import { HexInput } from '@hueycolor/svelte'
  ```
  ```svelte
  <script>
  import { HueyRoot, HexInput, hueyColor } from '@hueycolor/svelte'

  let color = $state(hueyColor('#1A3263'))
  </script>

  <HueyRoot bind:color>
    <HexInput />
  </HueyRoot>
  ```
  </TabItem>
</Tabs>

## Props

<PropsTable rows={[
  { name: 'alpha', type: 'boolean', default: 'false', description: 'Whether to include the alpha channel in the hex value. Set to true to display an 8-digit hex with the alpha channel.' },
]} />

Extends all standard HTML `input` attributes (`id`, `placeholder`, `disabled`, etc.).

### Format

Accepts both 3-digit and 6-digit hex values, with or without the `#` prefix. Use the up/down arrow keys to bump hex pair values. Hold Shift to bump by 10.

## Styling

Target with `[huey-input="hex"]`. The component renders a bare `<input>` and exposes no CSS variables of its own.

### With a label

`HexInput` is a single `<input>` element, so wrap it in a `<label>` (or pair it with one via `id`) to add a visible label and keep it accessible.

<Tabs syncKey="framework">
  <TabItem label="Vue">
  ```vue
  <label>
    <span>Hex</span>
    <HexInput />
  </label>
  ```
  </TabItem>
  <TabItem label="Svelte">
  ```svelte
  <label>
    <span>Hex</span>
    <HexInput />
  </label>
  ```
  </TabItem>
</Tabs>
